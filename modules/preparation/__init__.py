"""Offer Preparation module (Module 2).

Prepares the offers discovered by Module 1: captures each offer page,
populates the company/location catalogs (AI classifies only the location),
verifies duplication in two stages and closes the run with its own metrics,
executing the Preparation flow defined in the technical sheet.
"""
